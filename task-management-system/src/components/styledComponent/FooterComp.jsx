import { List, ListItem } from "@chakra-ui/react";
import styled from "@emotion/styled";

export const ListIt = styled(ListItem)`
    margin-bottom: 10px;
`

export const ListMain = styled(List)`
    text-align: start;
`